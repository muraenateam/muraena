'use strict';
// Smoke test: requires local puppeteer. Serves a fixture page with a login form,
// runs recon.js against it, asserts the JSON contains the password selector.
// Left as an integration check to run manually: `npm test` inside puppeteer/.
console.log('manual integration test — see plan');
